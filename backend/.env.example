# Database connection string
# Replace with your actual PostgreSQL credentials
DATABASE_URL="postgresql://username:password@localhost:5432/mindful_money?schema=public"

# Server port
PORT=5000

# CORS allowed origins (React app URL)
CORS_ORIGIN=http://localhost:3000
